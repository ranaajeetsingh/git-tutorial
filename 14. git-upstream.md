# Git Upstream

Used to add upstream forked repository to sync owned copy.

`git remote add upstream <repo_url>`

**To Download Upstream Changes**

`git fetch upstream`

**To Integrate Upstream Changes**

`git merge upstream/<branch_name>`

**To Download and Integrate Upstream Changes**

`git pull upstream <branch_name>`